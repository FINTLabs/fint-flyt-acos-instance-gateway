fint:
  application-id: fint-flyt-acos-instance-gateway
server:
  error:
    include-message: always
spring:
  profiles:
    include:
      - flyt-kafka
      - flyt-logging
      - flyt-resource-server